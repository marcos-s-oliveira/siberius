# Variáveis de ambiente do Siberius

# ==== SERVIDOR ====
# Porta do servidor API
API_PORT=3000

# Ambiente (development, production, test)
NODE_ENV=development

# ==== BANCO DE DADOS ====
# URL de conexão com o banco de dados PostgreSQL
# Formato: postgresql://usuario:senha@host:porta/database
DATABASE_URL="postgresql://user:password@localhost:5432/siberius?schema=public"

# ==== SEGURANÇA ====
# Chave secreta para geração de tokens JWT (OBRIGATÓRIA)
# Altere para um valor único e seguro em produção (mínimo 32 caracteres)
JWT_SECRET="your-super-secret-jwt-key-change-in-production"

# Tempo de expiração do token JWT
JWT_EXPIRES_IN=12h

# ==== INDEXADOR DE PDFs ====
# Diretório raiz onde os arquivos PDF serão monitorados
# O sistema irá escanear RECURSIVAMENTE todas as subpastas
# Windows: C:\ServiceOrder ou \\servidor\compartilhamento\pasta
# Linux: /mnt/pdfs ou /home/usuario/documentos
# IMPORTANTE: Use caminho absoluto em produção para evitar problemas
PDF_DIRECTORY="C:\ServiceOrder"

# Intervalo de verificação em minutos
# Define a frequência com que o sistema verifica novos arquivos
CHECK_INTERVAL_MINUTES=10

# Logging detalhado (true/false)
# Ativa logs mais detalhados para debugging
VERBOSE_LOGGING=true

# ==== CORS (Opcional) ====
# Origem permitida para requisições (frontend)
# Em produção, configure com o domínio/IP do frontend
# CORS_ORIGIN=http://192.168.1.100:5173
